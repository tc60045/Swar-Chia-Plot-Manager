import os
# probaby going to need to run Popen
# create the torrents - will take 20m
# transmission-create size 16384 
# add the torrent -- also takes some time
# transmission-remote localhost:port --auth 'localauth:localpass' -a /drive/*
# should be auto-started, but if not
# tranmission-remote
# can add and remove torrents with clutch
